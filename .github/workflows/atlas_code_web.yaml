name: atlas_code_web

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    paths:
      - ".github/workflows/atlas_code_web.yaml"
      - "atlas_code_web/**"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/atlas_code_web.yaml"
      - "atlas_code_web/**"

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      flutter_version: 3.41.1
      working_directory: atlas_code_web

