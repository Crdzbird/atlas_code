# 0.1.0

Initial release.

- Platform interface with a single contract: `localizedCountryNames(languageTag)` returning OS-localized names keyed by uppercase alpha-2 code, plus the shared `isoAlpha2Codes` list and a default method-channel implementation.
