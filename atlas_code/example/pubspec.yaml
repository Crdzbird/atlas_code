name: atlas_code_example
description: Demonstrates how to use the atlas_code plugin.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.12.0
resolution: workspace

dependencies:
  atlas_code: ^0.1.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  very_good_analysis: ^10.2.0

flutter:
  uses-material-design: true
